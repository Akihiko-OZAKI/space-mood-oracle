# ログ確認: ⭐マーク付きログの見つけ方

## 🔍 探すべきログ

追加した詳細ログには **⭐** マークが付いています。

---

## 📋 確認すべきログ

### サーバー起動時（`02:42:47 PM` あたり）

`[Database] Initializing MySQL pool with TLS to` の後に、以下のログが表示されるはずです：

```
[Database] Database name: test
[Database] Host: gateway01.ap-northeast-1.prod.aws.tidbcloud.com
[Database] Port: 4000
[Database] User: ...
[Database] Connection object created successfully.
[Database] Database connection pool ready for: test
[Database] ⭐ Current database (from SELECT DATABASE()): test
[Database] ⭐ Tables in current database: [...]
[Database] ⭐ space_weather_data exists? true/false
```

---

### 「実データ取得」実行時（`02:42:57 PM` あたり）

`[SpaceWeather] Failed to save real space weather data to database` の前に、以下のログが表示されるはずです：

```
[SpaceWeather] saveRealDataToDatabase called with X items
[SpaceWeather] Database connection obtained, starting to save data
[SpaceWeather] ⭐ Starting table verification...
[SpaceWeather] ⭐ Current database: test
[SpaceWeather] ⭐ Available tables: [...]
[SpaceWeather] ⭐ space_weather_data in tables? true/false
```

---

## 🔧 ログが見つからない場合

### 方法1: 検索機能を使う

RenderのLogsタブに検索ボックスがある場合、以下を検索：

```
⭐
```

または

```
Current database
```

---

### 方法2: ログをスクロール

1. RenderのLogsタブで、**一番上までスクロール**
2. `02:42:47 PM` あたりのログを見る
3. `[Database] Initializing MySQL pool with TLS to` の**後**のログを確認

---

### 方法3: エラーログの前後を確認

`02:42:57 PM` のエラーログの**前**（少し上）を見て、⭐マークが付いたログを探してください。

---

## 💡 もしログが表示されない場合

コードが正しくデプロイされていない可能性があります。以下を確認：

1. **Eventsタブでデプロイが完了しているか確認**
   - 最新のデプロイが "Live" になっているか

2. **ビルドエラーがないか確認**
   - Eventsタブで、ビルドエラーが表示されていないか

3. **コードが正しくコミットされているか確認**
   - GitHubで、最新のコミットに `server/db.ts` と `server/realSpaceWeather.ts` の変更が含まれているか

---

**まず、RenderのLogsタブで `⭐` を検索するか、`02:42:47 PM` あたりのログを詳しく見てください！** 🔍

