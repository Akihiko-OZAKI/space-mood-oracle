# 🔧 静的ファイルパスの修正

## ❌ 問題

デプロイ時に以下のエラーが発生:

```
Error: ENOENT: no such file or directory, stat '/opt/render/project/dist/public/index.html'
```

**原因**: 静的ファイルのパス解決が間違っている

## ✅ 修正内容

### 変更前

```typescript
const distPath = path.resolve(import.meta.dirname, "../..", "dist", "public");
```

これは、`dist/index.js` から見て間違ったパスを参照していました。

### 変更後

```typescript
const distPath = path.resolve(import.meta.dirname, "public");
```

`dist/index.js` から見て、`dist/public` は同じディレクトリにあるので、`public` だけを指定します。

## 🚀 デプロイ手順

### 1. 変更をコミット

```bash
git add server/_core/vite.ts
git commit -m "Fix: Correct static file path resolution for production"
```

### 2. GitHubにプッシュ

```bash
git push origin main
```

### 3. Renderで自動再デプロイ

- GitHubにプッシュすると、Renderが自動的に再デプロイを開始します
- ダッシュボードでデプロイ状況を確認

## ✅ 確認事項

再デプロイ後、以下を確認:

1. ✅ エラーが消えている
2. ✅ アプリが正常に表示される
3. ✅ 静的ファイルが読み込まれている

---

**変更をコミットして、再デプロイしましょう！** 🚀


